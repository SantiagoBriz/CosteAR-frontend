# En desarrollo dejar VACÍO (Vite proxea /api → http://localhost:3000).
# En producción (Vercel) setear la URL del backend desplegado, ej:
#   VITE_API_URL=https://costear-backend.up.railway.app
VITE_API_URL=
